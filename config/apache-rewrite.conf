# ABOUTME: Aktiviert mod_rewrite für WordPress Pretty Permalinks und REST-API.
# ABOUTME: Wird in docker-compose.yml als Volume in sites-available/000-default.conf eingehängt.
<Directory /var/www/html>
    AllowOverride All
    Options -Indexes
</Directory>
